development:
  host: localhost
  port: 5432
  database: trading_bot_dev
  username: postgres
  password: postgres
  pool: 5
  timeout: 5000

test:
  host: localhost
  port: 5432
  database: trading_bot_test
  username: postgres
  password: postgres
  pool: 5
  timeout: 5000

production:
  host: ${DB_HOST}
  port: ${DB_PORT}
  database: ${DB_NAME}
  username: ${DB_USER}
  password: ${DB_PASSWORD}
  pool: ${DB_POOL:-5}
  timeout: ${DB_TIMEOUT:-5000}
